using UnityEngine;
/// <summary>
/// This script is just a marker to indicate that the associated GameObject
/// requires a fixed rotation of -90 degrees when dropped by the NPCNavMesh.
/// </summary>
public class RequireFixedRotation : MonoBehaviour
{
}
